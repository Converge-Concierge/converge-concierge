## Packages
framer-motion | Smooth entry animations and micro-interactions

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

No backend API integration implemented yet; all forms and buttons simulate UI state only.
Routing uses absolute paths for wouter nested routes.
